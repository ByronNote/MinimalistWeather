def retrofitVersion = "2.5.0"
def okHttpVersion = "3.12.13"
def stethoVersion = "1.5.0"
def daggerVersion = "2.60.1"
def ormLiteVersion = "4.48"
def fastJsonVersion = "1.1.60.android"
def lifecycleVersion = "2.10.0"
def coroutinesVersion = "1.10.2"

project.ext {
    android = [
            compileSdkVersion: 37,
            applicationId    : "cn.byronlab.weather",
            minSdkVersion    : 23,
            targetSdkVersion : 37,
            versionCode      : 1,
            versionName      : "1.0"
    ]

    dependencies = [
            "annotation"                  : "androidx.annotation:annotation:1.9.1",

            // Legacy data adapter
            "rxjava"                      : "io.reactivex:rxjava:1.3.8",
            "retrofit"                    : "com.squareup.retrofit2:retrofit:${retrofitVersion}",
            "adapter-rxjava"              : "com.squareup.retrofit2:adapter-rxjava:${retrofitVersion}",
            "okhttp"                      : "com.squareup.okhttp3:okhttp:${okHttpVersion}",
            "okhttp3-logging-interceptor" : "com.squareup.okhttp3:logging-interceptor:${okHttpVersion}",
            "ormlite-android"             : "com.j256.ormlite:ormlite-android:${ormLiteVersion}",
            "fastjson"                    : "com.alibaba:fastjson:${fastJsonVersion}",
            "retrofit2-fastjson-converter": "com.github.BaronZ88:Retrofit2-FastJson-Converter:1.2",

            // Modern app stack
            "hilt-android"                : "com.google.dagger:hilt-android:${daggerVersion}",
            "hilt-compiler"               : "com.google.dagger:hilt-compiler:${daggerVersion}",
            "stetho"                      : "com.facebook.stetho:stetho:${stethoVersion}",
            "stetho-okhttp3"              : "com.facebook.stetho:stetho-okhttp3:${stethoVersion}",
            "activity-compose"            : "androidx.activity:activity-compose:1.13.0",
            "compose-bom"                 : "androidx.compose:compose-bom:2026.06.00",
            "lifecycle-runtime-compose"   : "androidx.lifecycle:lifecycle-runtime-compose:${lifecycleVersion}",
            "lifecycle-runtime-ktx"       : "androidx.lifecycle:lifecycle-runtime-ktx:${lifecycleVersion}",
            "lifecycle-viewmodel-compose" : "androidx.lifecycle:lifecycle-viewmodel-compose:${lifecycleVersion}",
            "lifecycle-viewmodel-ktx"     : "androidx.lifecycle:lifecycle-viewmodel-ktx:${lifecycleVersion}",
            "kotlinx-coroutines-android"  : "org.jetbrains.kotlinx:kotlinx-coroutines-android:${coroutinesVersion}"
    ]
}
